<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  ~ Copyright (c) Huawei Technologies Co., Ltd. 2025-2025. All rights reserved.
  -->


<mapper namespace="com.openjiuwen.studio.agent.manager.mapper.md.UserModelServiceProviderMapper">
    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        INSERT INTO t_user_model_service_provider(
            ID, PROVIDER_NAME, PROVIDER_NAME_EN, DESCRIPTION, TAGS,
            PROVIDER_URL, CREATED_DATE, LAST_UPDATED_DATE, LOGO, STATUS,
            DOMAIN_ID, PROJECT_ID, WORKSPACE_ID, CREATED_BY_USER, LAST_UPDATED_BY_USER, IDENTITY_ID
        ) VALUES (
                     #{id}, #{providerName}, #{providerNameEn}, #{description}, #{tags},
                     #{providerUrl}, #{createdDate}, #{lastUpdatedDate}, #{logo}, #{status},
                     #{domainId}, #{projectId}, #{workspaceId}, #{createdByUser}, #{lastUpdatedByUser}, #{identityId}
                 )
    </insert>

    <!-- 删除记录 -->
    <delete id="deleteById" parameterType="String">
        DELETE FROM t_user_model_service_provider WHERE ID = #{id}
    </delete>

    <!-- 更新记录 -->
    <update id="updateById" parameterType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        UPDATE t_user_model_service_provider SET
         PROVIDER_NAME = #{providerName},
         PROVIDER_NAME_EN = #{providerNameEn},
         DESCRIPTION = #{description},
         TAGS = #{tags},
         PROVIDER_URL = #{providerUrl},
         LAST_UPDATED_DATE = #{lastUpdatedDate},
         LOGO = #{logo},
         STATUS = #{status},
         LAST_UPDATED_BY_USER = #{lastUpdatedByUser}
        WHERE ID = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider WHERE ID = #{id}
        ORDER BY LAST_UPDATED_DATE DESC
    </select>
    <select id="selectByIds" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider
        WHERE ID IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 在指定项目+工作空间内按 id 集合查询（workspace-scoped；含 SYSTEM 空间以兼容系统供应商） -->
    <select id="selectByProjectIdAndWorkspaceId" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider
        WHERE ID IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND PROJECT_ID = #{projectId}
        AND (WORKSPACE_ID = #{workspaceId} OR WORKSPACE_ID = 'SYSTEM')
    </select>

    <!-- 根据PROJECT_ID查询 -->
    <select id="selectByProjectId" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider WHERE PROJECT_ID = #{projectId}
    </select>

    <select id="selectUserDataByName" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider WHERE PROJECT_ID = #{projectId} and (WORKSPACE_ID = #{workspaceId} or WORKSPACE_ID = 'SYSTEM')  and PROVIDER_NAME = #{providerName}
    </select>

    <sql id="conditionSql">
        <if test="condition.query != null and condition.query != '' ">
            and  (
            p.PROVIDER_NAME like CONCAT('%', #{condition.query}, '%')
            or p.PROVIDER_NAME_EN like CONCAT('%', #{condition.query}, '%')
            or p.DESCRIPTION like CONCAT('%', #{condition.query}, '%')
            or p.TAGS like CONCAT('%', #{condition.query}, '%')
            )
        </if>
        <if test="condition.id != null and condition.id != ''">
            and p.ID like  CONCAT('%', #{condition.id}, '%')
        </if>
        <if test="condition.providerIds != null and condition.providerIds.size() > 0" >
            and p.ID in
            <foreach collection="condition.providerIds" item="pid" open="(" close=")" separator=",">
                #{pid}
            </foreach>
        </if>
    </sql>

    <select id="queryByCondition" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProviderDetail">
        select p.*, m.ID as auth_meta_ids, m.AUTH_TYPE as auth_types
        from t_user_model_service_provider p
        left join t_provider_auth_metadata m on p.ID = m.PROVIDER_ID
        where  p.PROJECT_ID = #{condition.projectId} and (p.WORKSPACE_ID = #{condition.workspaceId} or p.WORKSPACE_ID = 'SYSTEM')
        <include refid="conditionSql"/>
        order by p.LAST_UPDATED_DATE DESC
    </select>

    <insert id="insertBackup" parameterType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        INSERT INTO t_user_model_service_provider_backup(
            ID, PROVIDER_NAME, PROVIDER_NAME_EN, DESCRIPTION, TAGS,
            PROVIDER_URL, CREATED_DATE, LAST_UPDATED_DATE, LOGO, STATUS,
            DOMAIN_ID, PROJECT_ID, WORKSPACE_ID, CREATED_BY_USER, LAST_UPDATED_BY_USER, IDENTITY_ID
        ) VALUES (
                     #{id}, #{providerName}, #{providerNameEn}, #{description}, #{tags},
                     #{providerUrl}, #{createdDate}, #{lastUpdatedDate}, #{logo}, #{status},
                     #{domainId}, #{projectId}, #{workspaceId}, #{createdByUser}, #{lastUpdatedByUser}, #{identityId}
                 )
    </insert>

    <select id="queryBackupByUpdateTime" resultType="com.openjiuwen.studio.agent.manager.entity.md.ModelServiceProvider">
        SELECT * FROM t_user_model_service_provider_backup WHERE LAST_UPDATED_DATE &lt; #{lastUpdatedDate}
        ORDER BY LAST_UPDATED_DATE DESC
    </select>

    <delete id="deleteBackupByIds">
        DELETE FROM t_user_model_service_provider_backup
        WHERE id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
