<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) Huawei Technologies Co., Ltd. 2025-2025. All rights reserved.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openjiuwen.studio.agent.manager.mapper.ShareResourceMapper">
    <resultMap id="BaseResultMap" type="com.openjiuwen.studio.agent.manager.entity.ShareResourceEntity">
        <id column="resource_id" jdbcType="VARCHAR" property="resourceId"/>
        <result column="resource_name" jdbcType="VARCHAR" property="resourceName"/>
        <result column="resource_type" jdbcType="VARCHAR" property="resourceType"/>
        <result column="workspace_id" jdbcType="VARCHAR" property="workspaceId"/>
        <result column="workspace_name" jdbcType="VARCHAR" property="workspaceName"/>
        <result column="trace_id" jdbcType="VARCHAR" property="traceId"/>
        <result column="version_list" jdbcType="VARCHAR" property="versionList"/>
        <result column="project_id" jdbcType="VARCHAR" property="projectId"/>
        <result column="tenant_id" jdbcType="VARCHAR" property="tenantId"/>
        <result column="creator_id" jdbcType="VARCHAR" property="creatorId"/>
        <result column="creator" jdbcType="VARCHAR" property="creator"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="updater_id" jdbcType="VARCHAR" property="updaterId"/>
        <result column="updater" jdbcType="VARCHAR" property="updater"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        resource_id,
        resource_name,
        resource_type,
        workspace_id,
        workspace_name,
        trace_id,
        version_list,
        project_id,
        tenant_id,
        creator,
        creator_id,
        create_time,
        updater_id,
        updater,
        update_time
    </sql>

    <!--
      卡片渲染同源过滤（与 queryWorkflowResource / queryControllerResource 的装配规则一致）：
      1. 活表存在性过滤：活表无行的共享项在列表装配阶段会被跳过（卡片不渲染），
         此处前置过滤使 count 与实际渲染的卡片数一致；
      2. 活名搜索：卡片名称取活表现值，搜索也只认活名，保证"显示什么就能搜到什么"，
         共享表快照名 resource_name 不再参与 controller/workflow 的搜索；
      3. 存在性条件镜像装配阶段查询（selectByWorkflowIds / selectByAgentIds）：
         workflow = t_agent_workflow（project_id + id，无 deleted 条件）；
         controller = t_agent（project_id + agent_id + deleted = false + type = 'controller'）；
      4. plugin/mcp/prompt 前端不会传入本查询（资产广场共享页仅 all/controller/workflow），
         其名称依赖版本级查询无法在 SQL 表达，保持原快照搜索语义。
    -->
    <sql id="liveResourceFilter">
        <choose>
            <when test="resourceType == 'workflow'">
                and exists (select 1 from t_agent_workflow w
                where w.id = t_share_resource.resource_id and w.project_id = #{projectId})
                <if test="resourceName != null and resourceName != ''">
                    and exists (select 1 from t_agent_workflow w
                    where w.id = t_share_resource.resource_id and w.project_id = #{projectId}
                    and w.name like concat('%', trim(lower(#{resourceName})), '%'))
                </if>
            </when>
            <when test="resourceType == 'controller'">
                and exists (select 1 from t_agent a
                where a.agent_id = t_share_resource.resource_id and a.project_id = #{projectId}
                and a.deleted = false and a.type = 'controller')
                <if test="resourceName != null and resourceName != ''">
                    and exists (select 1 from t_agent a
                    where a.agent_id = t_share_resource.resource_id and a.project_id = #{projectId}
                    and a.deleted = false and a.type = 'controller'
                    and a.name like concat('%', trim(lower(#{resourceName})), '%'))
                </if>
            </when>
            <when test="resourceType == 'all'">
                and (exists (select 1 from t_agent_workflow w
                where w.id = t_share_resource.resource_id and w.project_id = #{projectId})
                or exists (select 1 from t_agent a
                where a.agent_id = t_share_resource.resource_id and a.project_id = #{projectId}
                and a.deleted = false and a.type = 'controller'))
                <if test="resourceName != null and resourceName != ''">
                    and (exists (select 1 from t_agent_workflow w
                    where w.id = t_share_resource.resource_id and w.project_id = #{projectId}
                    and w.name like concat('%', trim(lower(#{resourceName})), '%'))
                    or exists (select 1 from t_agent a
                    where a.agent_id = t_share_resource.resource_id and a.project_id = #{projectId}
                    and a.deleted = false and a.type = 'controller'
                    and a.name like concat('%', trim(lower(#{resourceName})), '%')))
                </if>
            </when>
            <otherwise>
                <if test="resourceName != null and resourceName != ''">
                    and resource_name like concat('%', trim(lower(#{resourceName})), '%')
                </if>
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" parameterType="com.openjiuwen.studio.agent.manager.entity.ShareResourceEntity">
        insert into t_share_resource (resource_id, resource_name, resource_type, workspace_id, workspace_name, trace_id, version_list,
                                      project_id, tenant_id, creator, creator_id, create_time, updater, updater_id,
                                      update_time)
        values (#{resourceId}, #{resourceName}, #{resourceType}, #{workspaceId}, #{workspaceName}, #{traceId}, #{versionList},
                #{projectId}, #{tenantId}, #{creator},
                #{creatorId}, #{createTime}, #{updater}, #{updaterId}, #{updateTime})
    </insert>

    <select id="selectShareResourceEntityById" parameterType="map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where project_id = #{projectId}
        and resource_id = #{resourceId}
        and workspace_id = #{workspaceId}
    </select>

    <select id="selectSharedResourceByAuthWorkspaceIdAndType" parameterType="map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where resource_id in
        (
        select resource_id
        from t_share_scope
        where project_id = #{projectId}
        and (workspace_id = #{authWorkspaceId} or (workspace_id = 'all'))
        )
        <if test="resourceType == 'all'">
            and (resource_type = 'controller' or resource_type = 'workflow')
        </if>
        <if test="resourceType != 'all'">
            and resource_type = #{resourceType}
        </if>
        <include refid="liveResourceFilter"/>
            and workspace_id != #{authWorkspaceId}
        order by update_time desc
    </select>

    <select id="selectSharedResourceByTraceIdAndAuthWorkspace" parameterType="map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where trace_id = #{traceId}
        and resource_type = #{resourceType}
        and project_id = #{projectId}
        and resource_id in
        (
        select resource_id
        from t_share_scope
        where project_id = #{projectId}
        and (workspace_id = #{authWorkspaceId} or (workspace_id = 'all'))
        )
        and workspace_id != #{authWorkspaceId}
        order by update_time desc
    </select>


    <select id="selectShareResourceByResourceIds" parameterType="java.util.List" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where resource_id in
        <foreach collection="resourceIds" item="resourceId" open="(" separator="," close=")">
            #{resourceId}
        </foreach>
    </select>

    <select id="selectShareResourceEntityByResourceId"
            resultType="com.openjiuwen.studio.agent.manager.entity.ShareResourceEntity">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where resource_id = #{resourceId}
    </select>

    <select id="selectSharResourceEntityByWorkspaceId"
            resultType="com.openjiuwen.studio.agent.manager.entity.ShareResourceEntity">
        select
        <include refid="Base_Column_List"/>
        from t_share_resource
        where project_id = #{projectId}
        and workspace_id = #{workspaceId}
        <if test="resourceType == 'all'">
            and (resource_type = 'controller' or resource_type = 'workflow')
        </if>
        <if test="resourceType != 'all'">
            and resource_type = #{resourceType}
        </if>
        <include refid="liveResourceFilter"/>
        order by update_time desc
    </select>

    <select id="countResourceByResourceId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_share_resource
        WHERE project_id = #{projectId}
          AND resource_id = #{resourceId}
    </select>

    <update id="updateShareResourceEntity"
            parameterType="com.openjiuwen.studio.agent.manager.entity.ShareResourceEntity">
        update t_share_resource
        <set>
            <if test="versionList != null">
                version_list = #{versionList,jdbcType=VARCHAR},
            </if>
            <if test="updaterId != null">
                updater_id = #{updaterId,jdbcType=VARCHAR},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime,jdbcType=VARCHAR},
            </if>
        </set>
        where resource_id = #{resourceId,jdbcType=VARCHAR}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from t_share_resource
        where resource_id = #{resourceId,jdbcType=VARCHAR}
    </delete>
</mapper>
